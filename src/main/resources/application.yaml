spring:
  application:
    name: thietbicongnghe

  datasource:
    url: jdbc:mysql://localhost:3306/thietbicongnghe
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html